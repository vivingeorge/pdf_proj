Run Server : cd pdf_proj/mysite && python manage.py runserver


python manage.py makemigrations
python manage.py migrate


Image library
pip install Pillow (pip install Pillow --upgrade)
OR
brew install imagemagick (In order to have it installed to work with pdf's we need to have freetype and ghostscript packages)
sudo apt install linuxbrew-wrapper
sudo apt-get install perl
sudo apt-get install build-essential
brew doctor

pip install Wand